# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


## TODO: Implement these
GET      /card/:cardId               controllers.PatientCreditCardController.getCreditCardInfo(cardId: Long)

GET     /card                       controllers.PatientCreditCardController.getPatientCreditCard()

DELETE   /card/:cardId    controllers.PatientCreditCardController.deletePatientCreditCard(addressId: Long)

POST     /card               controllers.PatientCreditCardController.putPatientCreditCard()
